# DABO — Calendrier V2.1

Copier les dossiers `app`, `lib` et `tests` à la racine du projet `dabo-app` en acceptant le remplacement des fichiers.

Aucune migration Supabase.

Puis exécuter :

```powershell
npm run verify
```

Vérifier ensuite en production : sélection d'une date avec et sans événement, distinction Aujourd'hui/date sélectionnée, événements Foyer/Personnel, navigation entre les mois, mode clair et sombre, mobile et desktop.
