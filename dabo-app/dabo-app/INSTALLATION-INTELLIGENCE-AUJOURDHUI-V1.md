# DABO V2 — Intelligence sur Aujourd’hui V1

Copier `app`, `lib` et `tests` à la racine de `dabo-app` et accepter fusion/remplacement.

Aucune migration Supabase. Aucune nouvelle dépendance.

Puis lancer `npm run verify`.
