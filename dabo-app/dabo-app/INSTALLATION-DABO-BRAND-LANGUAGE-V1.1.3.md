# DABO Brand Language V1.1.3 — correctif final des tests

Copier le dossier `tests` à la racine de `dabo-app` et accepter le remplacement.

Ce patch modifie uniquement `tests/brand-language-v1-1.test.ts`.
Aucune modification de production, aucune migration Supabase, aucune traduction.

Puis lancer :

```powershell
npm run verify
```

Cible : 194/194 tests, i18n 606 × 7, TypeScript PASS, Build PASS.
