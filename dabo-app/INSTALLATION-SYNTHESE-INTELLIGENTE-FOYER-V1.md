# DABO V2 — Synthèse intelligente du foyer V1

Copier les dossiers `app`, `lib` et `tests` à la racine de `dabo-app` en acceptant la fusion/remplacement.

Aucune migration Supabase. Aucune nouvelle dépendance.

Puis lancer :

```powershell
npm run verify
```
