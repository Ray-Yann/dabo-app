# DABO — Traction V1

1. Copier le contenu de ce dossier à la racine de `dabo-app`.
2. Accepter le remplacement de `app/admin/page.tsx`.
3. Lancer `npm run verify`.
4. Si tout passe, commit/push via GitHub Desktop puis vérifier Vercel et la production.

Aucune migration SQL. Aucune variable d'environnement. Cette version réutilise exclusivement les KPI déjà calculés par le cockpit Admin.
